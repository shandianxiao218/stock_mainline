# 待办事项

## 已完成

- [x] 记录项目决策到项目记忆。
- [x] 起草 MVP 技术设计。
- [x] 起草数据库 DDL。
- [x] 起草 API 草案。
- [x] 搭建可运行的本地 Web/API demo。
- [x] 增加 Excel 导出路径。
- [x] 增加东方财富本地二进制日线 C 导入器。
- [x] 将东方财富 `daily_quotes.csv` 装载进本地 SQLite。
- [x] 在数据源状态 API 中展示 SQLite 入库状态。
- [x] 用 SQLite 日线驱动主线榜单、详情和持仓风险。
- [x] 在详情页展示主线成分股表现。
- [x] 实现基于 SQLite 可用交易日的真实回测接口。
- [x] 支持保存单日复盘结果到 SQLite。
- [x] 在 Web 页面增加保存复盘按钮。
- [x] 增加模型参数配置与版本管理 API/UI，并接入主线评分公式。
- [x] 增加因子有效性分析 API/UI：5 日/20 日 IC、Rank IC、双窗口方向和动态权重建议。
- [x] 增加置信度拆解和近 20 日历史 API/UI。
- [x] 增加本地日志审计：API 访问、参数修改、复盘保存、回测、自选股和持仓变更。
- [x] 增加单主线近 20 日风险信号历史 API/UI。
- [x] 增强回测页面：模型版本、区间、持有期、Top N 配置和样本 CSV 下载。
- [x] 增加本地权限角色：访客、普通用户、研究员、管理员、审计员，并对写操作做权限校验。
- [x] 增加新闻催化事件库和人工催化等级录入/查看 API/UI。
- [x] 增加评分、风险扣分、置信度、主线矩阵和因子有效性本地烟测。
- [x] 解析东方财富 `hs_bk_crc_data_new.dat` 并导入真实板块成分到 SQLite。
- [x] 增加东方财富真实板块浏览和成分股查看 API/UI。
- [x] 增加近 20 日主线矩阵。
- [x] 选择日期后自动刷新主线榜单、详情、报告和持仓风险。
- [x] 完成前端回测功能展示，展示指标和逐日样本，而不是原始 JSON。
- [x] 完成可编辑自选股功能：新增、删除、持久化、风险联动。
- [x] 双击成分股显示 K 线。
- [x] 主线详情显示全部成分股。
- [x] 成分股列表展示当日 OCHL、成交量、成交额、涨幅、是否炸板、游资参与、近 5 日涨幅，并支持排序。
- [x] 对照 SRS v1.0 检查未实现功能，写入 TODO 并继续实现。
- [x] 增加板块日度行情快照构建脚本 `build_sector_snapshots.py`。
- [x] 增加涨停/触板/炸板/连板信号构建脚本 `build_limit_signals.py`。
- [x] 编写软件设计说明书，并在文档末尾增加 SRS 需求追踪表。
- [x] 补充软件设计说明书第 8 章评分模型公式和数据来源。
- [x] 按软件设计说明核对当前实现，并在设计文档末尾追加软件实现情况核对表。

---

## SRS v1.0 对照 — 未实现功能清单

以下按优先级排序，Phase 1-3 范围内的功能优先，Phase 4 暂不实现。

### P0：核心评分因子缺失（影响评分准确性）

- [x] **SRS 8.2 热度分 — 舆情边际变化率 (权重8)**：已将舆情因子权重按比例重分配给其他因子，待接入真实舆情源后恢复。
- [x] **SRS 8.2 热度分 — 舆情绝对热度 (权重4)**：同上。
- [x] **SRS 8.3 延续性分 — 舆情边际变化 (权重4)**：同上。
- [x] **SRS 8.4 风险扣分 — 炸板率过高 (0-4分)**：已基于 `local_limit_signal_daily` 真实炸板数据实现。
- [x] **SRS 8.4 风险扣分 — 监管/异动风险 (0-3分)**：已基于极端波动信号检测实现。

### P1：资金接力断裂分析（FR-007）

- [x] **SRS 9.6.1 领涨延续率**：已实现，加载前日涨跌幅计算延续率。
- [x] **SRS 9.6.2 涨停重合率**：已实现，计算今日涨停股中昨日也强势的比例。
- [x] **SRS 9.6.3 核心股偏离度**：已实现，按成交额排序取核心股。
- [x] **SRS 9.6 资金接力断裂扣分**：已综合三项指标按 SRS 规则表扣分（0-5分）。
- [x] **API + UI**：已增加 relay-break API 和详情页接力断裂展示。

### P2：板块成分管理（FR-002）

- [x] **SRS 6.2 板块成分历史版本**：导入时自动记录 as_of_date，不再清空历史。
- [x] **自定义板块创建**：已实现 custom-sector CRUD API。
- [x] **成分差异对比**：已实现 sector_diff API，按日期对比成分差异。

### P3：主线管理（FR-003）

- [x] **主线 CRUD**：已实现创建/编辑/归档/恢复 API。
- [x] **主线-底层板块映射管理**：已实现 sector mapping 管理。
- [x] **主线定义版本管理**：已实现 theme_version 审计表，记录操作人、时间和原因（SRS 7.4）。
- [x] **人工覆盖自动聚合**：已支持，通过主线映射优先于自动聚合。
- [x] **主线历史查询**：已实现 theme_history API。

### P4：真实板块接入与主线聚合

- [x] **东方财富真实板块成分入库**：已通过 C 导入器和 SQLite 装载 `em_sector`、`em_sector_constituent_history`。
- [x] **真实板块浏览 API/UI**：已提供 `/api/v1/sectors` 和 `/api/v1/sectors/{sector_code}/constituents`。
- [x] **修复无映射时全量评分超时**：无 `local_theme` 映射时使用内置主线-东方财富板块白名单，避免 1000+ 板块拖死首页矩阵和回测。
- [x] **真实板块参与评分的裁剪策略**：无人工映射时按当日成交、涨幅、广度和涨停近似筛选东方财富真实候选板块，不能直接全量逐日评分。
- [x] **股票名称真实显示**：东方财富 C 导入器已从 StkQuoteList 解析股票名称，按市场+代码入库，成分股表不再把名称显示成代码。
- [x] **默认主线名去模拟化**：无人工主线映射时不再输出固定 7 条模拟主线名，改为东方财富真实板块动态候选。
- [x] **自动聚合结果落库**：将候选底层板块聚合为上层主线，并写入本地主线映射版本。
- [x] **板块日度行情快照自动构建**：已集成到 load_eastmoney_csv.py 导入流程。
- [x] **涨停/触板/炸板信号自动构建**：已集成到 load_eastmoney_csv.py 导入流程。

### P5：榜单筛选与首页增强

- [x] **SRS 13.2 榜单筛选**：已增加置信度、状态、风险等级前端筛选。
- [x] **主线榜单默认裁剪**：默认只显示前 10 个板块，支持下拉选择前 100 和全部，避免真实板块候选过多拖慢页面和阅读。
- [x] **SRS 13.1 首页 — 退潮主线**：已增加退潮主线面板。
- [x] **SRS 13.1 首页 — 今日关键验证点**：已增加验证点列表。
- [x] **近 20 日主线矩阵默认裁剪**：默认显示目标日期有数据的前 10 个板块，支持下拉选择前 20、30、50、100 和全部，减少无当日数据板块造成的空白行。

### P6：详情页增强

- [x] **SRS 13.3 详情页 — 次日验证条件**：已增加验证条件列表。
- [x] **SRS 13.3 详情页 — 资金接力分析模块**：已增加接力断裂分析展示。
- [x] **评分计算可解释图表**：详情页展示主线分公式、关键统计、热度/延续性每项得分、权重、加权贡献和计算依据。
- [ ] **SRS 13.3 详情页 — 舆情热度趋势图**：接入真实舆情后展示趋势。

### P7：预警提醒系统（FR-015）

- [x] 新主线进入前 N 名提醒。
- [x] 主线分排名快速上升提醒。
- [x] 头部主线风险扣分快速上升提醒。
- [x] 核心股炸板提醒。
- [x] 资金接力断裂提醒。
- [x] 置信度下降提醒。
- [x] 高位放量滞涨提醒。
- [x] 预警 API 端点 + 前端预警面板。

### P8：因子动态权重自动接入

- [x] 将因子有效性分析的建议自动写入评分配置，按 SRS 10.1 公式 `0.85 × 基础 + 0.15 × 动态` 和 SRS 10.2 双窗口确认机制执行。
- [x] 单因子最大调整不超过基础权重 25%（SRS 10.4）。

### P9：舆情数据源

- [x] 接入低成本舆情源（行情代理：成交额/涨停率/涨幅）。
- [x] 实现舆情边际变化率计算。
- [x] 实现舆情绝对热度计算。
- [x] 实现舆情过热扣分（SRS 9.7.3）。
- [x] 实现舆情与价格/成交背离检测。

### P10：回测与历史数据增强

- [x] 导入 5 年东方财富日线，扩展真实回测样本。
- [ ] 完整 5 年历史数据导入和无未来函数回测。
- [x] 增加收盘/导入后的预计算任务：主线榜单、主线详情摘要、近 20 日矩阵、置信度历史、风险历史和常用成分股指标必须落到本地快照表。
- [x] 页面刷新 API 改为默认读取预计算快照；只有调试、参数实验和回测任务允许按需重算。
- [x] 为 5 年数据量建立 SQLite 性能索引：`trade_date`、`theme_id`、`sector_code`、`symbol`、`rank` 等查询路径。
- [x] 增加 100ms 页面刷新性能回归测试，覆盖首页榜单、主线详情首屏、20 日矩阵和风险历史。
- [x] 首页首屏去阻塞：主线矩阵、因子有效性、置信度历史、审计、催化、预警等模块改为后台异步加载，避免任一重计算接口慢时整页空白。

---

## 设计说明第 16/17 章后续执行 TODO

以下 TODO 来自 `docs/软件设计说明书.md` 第 16 章需求追踪表和第 17 章实现情况核对表。盘中实时监控按用户确认不实现。

### D16-P0：100ms 页面刷新与预计算快照

- [x] 建立 `local_theme_ranking_snapshot`：按交易日、交易周期保存主线榜单完整 JSON，默认 API 不再请求时重算。
- [x] 建立 `local_theme_matrix_snapshot`：按交易日和窗口天数保存近 20 日主线矩阵完整 JSON，支持前 10/20/30/50/100/全部裁剪。
- [x] 建立 `local_theme_detail_snapshot`：按交易日、主线 ID 保存详情首屏 JSON，包括评分拆解、强分支、核心股、成分股表现和解释图表数据。
- [x] 建立 `local_confidence_history_snapshot`：按交易日和窗口天数保存置信度拆解与历史。
- [x] 建立 `local_risk_history_snapshot`：按交易日、主线 ID 和窗口天数保存风险历史。
- [x] 建立 `local_factor_effectiveness_snapshot`：按交易日和窗口天数保存因子 IC、Rank IC、动态权重建议。
- [x] 增加快照构建脚本：导入或收盘后一次性生成榜单、矩阵、详情、置信度、风险和因子快照。
- [x] API 默认读取快照；仅当 `refresh=1` 或调试路径显式要求时才执行实时重算。
- [x] 页面返回中增加 `snapshot`、`snapshot_created_at`、`fallback_live` 字段，便于确认是否命中快照。
- [x] 为 `trade_date`、`theme_id`、`sector_code`、`symbol`、`rank`、`period`、`days` 建立 SQLite 查询索引。
- [x] 增加 100ms 性能回归脚本，覆盖首页榜单、主线详情首屏、20 日矩阵、置信度历史和风险历史。

### D16-P1：5 年历史数据与回测可信度

- [x] 使用 C 导入器从东方财富本地二进制文件导入至少 5 年日线数据，不使用 Python 读取二进制。
- [x] 增加 5 年数据覆盖检查：起止交易日、股票数量、日线数量、板块成分数量、快照日期数量。
- [x] 增加无未来函数检查：回测任一交易日只能读取当日及之前的行情、成分、主线定义、参数版本和快照。
- [x] 回测默认读取预计算日度评分结果；缺失时记录缺口，不静默重算为正式结果。
- [x] 建立 `local_backtest_run`：保存回测任务参数、状态、开始时间、结束时间、指标摘要和结果文件路径。
- [x] 增加异步回测 API：`async=true` 后后台执行，`/api/v1/backtest/runs/{task_id}` 查询状态。
- [x] 前端回测页改为异步轮询任务状态，避免长周期请求阻塞。
- [ ] 补齐板块成分历史版本；当前 5 年日线已导入，但东方财富板块成分只有当前快照，长周期回测仍有成分版本风险。

### D16-P2：模型数据源补齐

- [x] 接入真实舆情源或本地可导入舆情表 `local_sentiment_daily`，替换当前行情代理舆情。
- [ ] 舆情趋势图使用真实讨论量、新闻提及量、搜索热度或社区热度，不再只用成交/涨幅代理。
- [x] 催化事件按 S/A/B/C 等级、事件日期和衰减系数稳定计入热度分、延续性分和解释输出。
- [x] 自动聚合结果写入 `local_auto_theme_cluster`，记录聚合版本、包含底层板块、核心股、生成原因和生效日期。
- [ ] 回测时使用历史当时的自动聚合版本，保证主线定义可复现。
- [x] 接入龙虎榜或可导入游资数据，替换当前”游资参与”占位判断。
- [ ] 补充申万/中信/同花顺/通达信/Tushare 板块口径，支持多源板块对比。

### D16-P3：可用性、安全和测试

- [x] 增加导入、快照构建、回测任务失败重试和失败原因记录。
- [x] 增加本地任务运行日志和最近一次成功快照状态 API。
- [ ] 保留个人使用的轻量角色模型；若后续多人使用，再增加正式登录、会话和用户表。
- [x] 增加前端自动化测试，覆盖日期切换、榜单裁剪、矩阵倒序、详情评分解释、成分股排序和 K 线弹窗。
- [x] 增加 Excel 导出回归测试，确认榜单、回测样本和复盘报告可导出。

---

## 下一阶段执行 TODO（详细拆分）

以下将上述 D16-P1/P2/P3 中未完成项按**可立即执行**和**依赖外部数据源**分两批拆分。每项含具体实现步骤、涉及文件和验收标准。

### 批次 A：可立即执行（无外部数据依赖）

#### A1. 催化事件按 S/A/B/C 等级和衰减系数稳定计入评分（G-004）

> 对应 D16-P2 催化事件项，当前催化仅判断”有无”，不区分等级和时效。

- [x] **A1-1** 在 `catalyst_store.py` 增加按日期范围查询催化事件的函数 `get_catalysts_for_scoring`。
- [x] **A1-2** 在 `real_scoring.py` 的 `score_sector_from_db` 中替换催化占位逻辑：按 S/A/B/C 等级赋基础分（S=85, A=70, B=50, C=30），按 20 日线性衰减。
- [x] **A1-3** 催化持续性因子同样按等级衰减：取近 5 日催化分均值。
- [x] **A1-4** 在详情页评分解释中展示催化贡献。
- [x] **A1-5** 6 个催化评分单元测试全部通过。

#### A2. 自动聚合结果版本化落库（G-005）

> 对应 D16-P2 自动聚合项和 P4 未完成项。当前动态候选只在内存中，不复现。

- [x] **A2-1** 建表 `local_auto_theme_cluster`（`cluster_store.py`）。
- [x] **A2-2** `build_themes_for_date` 聚类后自动落库，同日重复执行版本号自增。
- [x] **A2-3** 增加 API `GET /api/v1/themes/auto-clusters?date=&version=`。
- [x] **A2-4** 评分引擎落库后自动版本化（回测时读取待增强）。
- [x] **A2-5** 快照构建脚本通过 `build_themes_for_date` 自动触发聚合落库。
- [x] **A2-6** 4 个聚合落库单元测试全部通过。

#### A3. 回测默认读取预计算日度评分（D16-P1）

> 当前回测每个交易日都实时重算评分，5 年数据极慢。应优先读取快照/预计算结果。

- [x] **A3-1** 新增 `local_backtest_daily_snapshot` 表，存储每日主题评分和成分股代码。
- [x] **A3-2** 快照命中时直接使用，不调用 `build_themes_for_date`。
- [x] **A3-3** 快照未命中时：实时计算并保存快照，标记 `fallback_live=true`。
- [x] **A3-4** 回测结束后返回 `fallback_count` 和 `gap_dates`。
- [x] **A3-5** 无未来函数测试验证回测结果包含缺口信息。

#### A4. 任务失败重试和失败原因记录（D16-P3）

> 当前快照构建或导入失败时无自动重试，失败原因仅日志。

- [x] **A4-1** 快照构建已有 try/except 和 build_log（原有实现）。
- [x] **A4-2** 增加 `--retry-failed` 参数，重试失败快照类型。
- [x] **A4-3** 异步回测任务增加最多 2 次失败重试。
- [x] **A4-4** `load_eastmoney_csv.py` 已有子步骤 try/except 和错误报告（原有实现）。
- [x] **A4-5** 增加 API `GET /api/v1/tasks/status`。

#### A5. 前端自动化测试（G-011）

- [x] **A5-1** 创建 `tests/test_frontend.py`（Playwright headless Chromium）。
- [x] **A5-2** 测试用例：页面加载 — 验证标题和榜单区域可见。
- [x] **A5-3** 测试用例：日期切换 — 验证内容更新。
- [x] **A5-4** 测试用例：榜单裁剪 — 验证裁剪控件存在。
- [x] **A5-5** 测试用例：详情页评分解释 — 点击榜单第一条验证详情区域。
- [x] **A5-6** 测试用例：K 线弹窗 — 双击成分股行验证弹窗出现。
- [x] **A5-7** 服务未运行时自动 skip。

#### A6. Excel 导出回归测试（D16-P3）

- [x] **A6-1** 在 `test_scoring.py` 中增加 `ExcelExportTest` 类。
- [x] **A6-2** 测试用例：榜单 Excel 数据可生成并写入 xlsx。
- [x] **A6-3** 测试用例：风险明细数据结构正确。
- [x] **A6-4** 测试用例：复盘报告数据可获取且非空。

#### A7. 无未来函数系统测试（17.10）

> 当前无未来函数检查只覆盖板块成分缺历史版本风险，需补充严格测试。

- [x] **A7-1** 在 `test_scoring.py` 增加 `NoFutureLeakTest` 类。
- [x] **A7-2** 测试用例：`load_histories` 不加载目标日期之后的数据。
- [x] **A7-3** 测试用例：`stock_metrics` 只基于传入历史数据计算。
- [x] **A7-4** 测试用例：回测结果包含缺口信息。
- [x] **A7-5** 测试用例：模型配置不使用未来数据。

### 批次 B：依赖外部数据源或长期投入

> 以下项需外部数据接入或较长时间，按优先级排列但暂不立即执行。

#### B1. 板块成分历史版本补齐（G-001）

- [ ] 定期（如每周）导出东方财富板块成分快照，累积 `em_sector_constituent_history` 历史记录。
- [ ] 探索东方财富是否有历史成分接口或历史 .dat 文件可回溯。
- [ ] 在回测中使用 `as_of_date` 约束成分版本，无版本时标记成分版本风险。

#### B2. 接入真实舆情源（G-003）

- [x] 调研可本地化导入的舆情源（AKShare 东财热度排行 `stock_hot_rank_em`）。
- [x] 设计热度入库表 `ak_hot_rank_daily` 和装载逻辑（`load_akshare_data.py`）。
- [x] 替换舆情评分为增强版（行情代理 + 热度排名融合）。
- [x] 前端详情页增加舆情热度趋势图（SRS 13.3）。

#### B3. 接入龙虎榜数据（G-007）

- [x] 接入 AKShare 龙虎榜接口 `stock_lhb_detail_em`。
- [x] 增加龙虎榜入库表 `ak_dragon_tiger_daily` 和装载逻辑。
- [x] 替换”游资参与”占位判断为真实龙虎榜数据。
- [x] 龙虎榜数据接入热度评分因子。

#### B4. 多源板块体系（G-008）

- [ ] 接入申万/中信/同花顺行业分类。
- [ ] 支持板块口径切换和多源对比 API。

#### B5. 正式登录与会话（G-009）

- [ ] 保留当前轻量角色模型不变。
- [ ] 仅在多人使用需求明确时再增加登录、会话和用户表。

#### B6. 异步回测长周期管理增强（G-010）

- [x] 增加取消正在运行的回测任务 API。
- [x] 增加回测进度百分比估算。
- [x] 回测结果支持下载为独立文件。

---

## 设计评审后新增 TODO

以下来自 `docs/设计评审问题答复.md`，优先解决会影响验收口径和误用风险的事项。

### R-P0：验收边界和防误用

- [x] 前端首页、详情页、回测页、复盘报告和 Excel 导出统一展示”研究辅助，不构成投资建议”免责声明。
- [x] 低置信度时弱化榜单视觉权重，显示”参考价值有限”，避免用户把排序当成确定性交易信号。
- [x] 次日验证点文案检查：不得出现买入、卖出、加仓、减仓等交易动作表达。
- [x] 在 README 和设计文档中明确当前服务不得暴露到公网或不可信局域网。

### R-P1：数据来源和可信度标签

- [x] API 统一增加 `data_quality` 或 `source_type` 字段，覆盖真实、近似、代理、人工、缺失五类。
- [x] 前端详情页每个模块展示数据来源标签：行情、舆情、涨停/炸板、催化、龙虎榜、聚合、成分股。
- [x] 涨停/触板/炸板风险和因子解释标注”日线近似”，不表达为盘口级真实封板质量。
- [x] 资金流相关表述改为”成交/价格同步性代理”，未接真实资金流前不写成真实资金流。

### R-P2：回测可复现和无未来函数

- [x] 回测读取当日有效的 `local_auto_theme_cluster` 自动聚合版本。
- [x] 回测读取当日有效的人工主线版本；历史修正必须显式写原因和审计。
- [x] 快照表记录依赖的模型参数版本、聚合版本、主线版本、数据可用时间。
- [x] 缺少板块成分历史版本时，回测结果降级为 warning，不能标记为完整无未来函数。

### R-P2.5：主线阶段有限状态机

- [x] 新增 `theme_stage_state_daily` 或等价表，按交易日、主线 ID、模型版本保存阶段状态。
- [x] 固定阶段枚举：启动、加速、高潮、分歧、退潮、修复；不允许自由文本作为主阶段。
- [x] 实现状态迁移约束：启动->加速/分歧/退潮，加速->高潮/分歧/退潮，高潮->分歧/退潮，分歧->修复/退潮/加速，退潮->修复/启动，修复->加速/分歧/退潮。
- [x] 阶段判断使用热度、延续性、风险扣分、3/5/10 日趋势、涨停质量、接力断裂、核心股结构、广度、成交额持续性和催化/舆情配合度。
- [x] API 返回 `stage`、`previous_stage`、`stage_reason`、`transition_signals`、`stage_confidence`。
- [x] 将现有 `status` 文案降级为阶段原因或风险标签，不再作为主阶段。
- [x] 回测和复盘读取当日落库阶段状态，不用最新规则静默改写历史。
- [x] 增加状态机单元测试：合法迁移、非法迁移、状态原因、低置信度、历史状态复现。

### R-P3：快照失效和任务机制

- [x] 建立快照依赖图：行情、板块、模型参数、主线映射、催化、舆情、龙虎榜变化后标记相关快照失效。
- [x] 增加快照重建队列和进度百分比，支持部分可用。
- [x] 增加快照失效状态 API，前端能看到”快照过期/构建中/构建失败”。
- [x] 冷启动后自动构建最新交易日必要快照，失败时保留明确失败原因。

### R-P4：API 规范和日期协议

- [ ] 编写 `docs/openapi.yaml`，覆盖当前所有 `/api/v1/*` 接口。
- [ ] 统一错误结构：`code`、`message`、`detail`、`request_id`。
- [ ] 所有日期相关 API 返回 `requested_date`、`resolved_trade_date`、`is_fallback`、`fallback_reason`。
- [ ] 榜单、矩阵、成分股、历史列表统一分页和排序参数：`limit`、`offset`、`order_by`、`order_dir`。
- [ ] 增加独立 `trading_calendar` 表，不再只依赖行情日期推导交易日。

### R-P5：正式产品化迁移预案

- [ ] 编写 SQLite 到 PostgreSQL 迁移说明，列出字段类型差异和索引差异。
- [ ] 设计正式登录、会话、用户表和权限表，替代 `X-User-Role`。
- [ ] 区分本地审计和不可篡改审计；商业化前补充外部审计或哈希链方案。
- [ ] 增加 C 导入器固定样本回归测试，校验股票名称、日期区间、行数和板块成分数量。

### 明确不实现（用户确认）

- [x] 盘中实时监控（分钟级行情、盘中主线热度更新、实时看板）不实现。
- [x] 盘中数据刷新（60 秒内）不实现。
